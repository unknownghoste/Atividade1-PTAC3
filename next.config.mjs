/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
        "www.ifms.edu.br"
        //colocar dominio do moodle para atividade. não precisa de https. fazer footer com link do moodle.
    ]
}
};

export default nextConfig;

